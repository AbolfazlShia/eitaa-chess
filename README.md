# 👑 شطرنج هخامنشی

یک بازی شطرنج آنلاین کامل با تم باستانی ایران و یکپارچه‌سازی با ربات ایتا.

## ✨ ویژگی‌ها

- 🎮 **سه حالت بازی:**
  - تک‌نفره با هوش مصنوعی (۵ سطح)
  - دعوت دوستان از طریق لینک
  - پیدا کردن رقیب شانسی

- 🎨 **تم هخامنشی:** طراحی زیبا با رنگ‌های طلایی و سنگی
- 💰 **سیستم سکه و لول:** کسب سکه با برنده شدن و ارتقای سطح تا ۳۰
- ⏱️ **تایمر:** زمان‌بندی استاندارد (۱۰ دقیقه + ۲ ثانیه)
- 💬 **پیام‌های آماده:** جملات از پیش تعریف شده در چند دسته
- 🔒 **حریم خصوصی:** نمایش فقط نام و عکس، بدون نام کاربری

## 🚀 راه‌اندازی

### پیش‌نیازها

- Node.js 18+
- pnpm (یا npm/yarn)

### نصب

```bash
# نصب وابستگی‌ها
pnpm install

# اجرای در حالت توسعه (فرانت + بک‌اند)
pnpm dev
```

- فرانت: http://localhost:5173
- بک‌اند: http://localhost:8787

### ساخت برای تولید

```bash
pnpm build
pnpm start
```

## 🔗 یکپارچه‌سازی با ایتا

برای استفاده با ربات ایتا:

1. دامنه سایت را در تنظیمات ربات ایتا وارد کنید
2. ربات به صورت خودکار پارامترهای زیر را به URL اضافه می‌کند:
   - `eitaa_id`: شناسه کاربر در ایتا
   - `name`: نام کاربر
   - `avatar_url`: آدرس عکس پروفایل

### مثال لینک

```
https://your-domain.com/?eitaa_id=12345&name=علی&avatar_url=https://...
```

### لینک دعوت

لینک دعوت به این شکل است:
```
https://your-domain.com/?invite=INVITE_ID&eitaa_id=...&name=...&avatar_url=...
```

## 🎯 قوانین بازی

- قوانین کامل شطرنج (chess.js)
- تایمر: ۱۰ دقیقه + ۲ ثانیه به ازای هر حرکت
- کیش و مات، پات، مساوی
- پروموشن پیاده

## 💰 سیستم سکه و لول

- **تک‌نفره:** بر اساس سطح AI (۱۰-۳۵ سکه)
- **دعوت:** ۳۰ سکه برای برنده
- **رقیب شانسی:** ۴۰ سکه برای برنده
- **لول:** هر ۱۰۰ سکه = یک سطح (حداکثر ۳۰)
- **محدودیت:** حداکثر ۱۰ مسابقه زنده در روز

## 🛠️ تکنولوژی‌ها

- **فرانت:** React + TypeScript + Vite
- **بک‌اند:** Node.js + Express + Socket.IO
- **شطرنج:** chess.js
- **AI:** Minimax با Alpha-Beta Pruning

## 📝 متغیرهای محیطی

```env
PORT=8787
DAILY_LIVE_CAP=10
```

## 📦 ساختار پروژه

```
.
├── server/          # بک‌اند (Express + Socket.IO)
│   └── src/
│       └── index.ts
├── web/             # فرانت (React)
│   └── src/
│       ├── modules/
│       │   ├── app/
│       │   ├── home/
│       │   └── play/
│       └── styles.css
└── package.json
```

## 🎨 تم هخامنشی

- رنگ‌های طلایی و سنگی
- فونت فارسی Vazirmatn
- طراحی مدرن با گرادیان‌ها و سایه‌ها
- ریسپانسیو برای موبایل

## 📱 PWA

برای تبدیل به PWA، فایل‌های `manifest.json` و service worker را اضافه کنید.

## 🔧 توسعه

```bash
# فقط فرانت
pnpm --filter web dev

# فقط بک‌اند
pnpm --filter server dev
```

## 📄 لایسنس

MIT

---

ساخته شده با ❤️ برای جامعه فارسی‌زبان
