# راهنمای نصب Xcode و حل مشکل Build

## 🔴 مشکل فعلی

شما فقط **Command Line Tools** نصب دارید، اما برای build کردن macOS app نیاز به **Xcode کامل** دارید.

## ✅ راه حل‌ها

### روش ۱: نصب Xcode کامل (برای ساخت App)

#### مراحل:

1. **باز کردن App Store:**
   - App Store را باز کنید
   - یا در Terminal: `open -a "App Store"`

2. **جستجوی Xcode:**
   - در App Store "Xcode" را جستجو کنید
   - یا این لینک را باز کنید: [Xcode در App Store](https://apps.apple.com/app/xcode/id497799835)

3. **نصب Xcode:**
   - روی "Get" یا "Install" کلیک کنید
   - ⚠️ **نکته:** Xcode حدود 12-15 GB فضا نیاز دارد
   - نصب ممکن است 30 دقیقه تا 2 ساعت طول بکشد

4. **تنظیم Xcode به عنوان Developer Directory:**
   ```bash
   sudo xcode-select --switch /Applications/Xcode.app/Contents/Developer
   ```

5. **قبول کردن License:**
   ```bash
   sudo xcodebuild -license accept
   ```

6. **اجرای دوباره Build Script:**
   ```bash
   ./build.sh
   ```

#### نکات مهم:
- Xcode رایگان است اما نیاز به Apple ID دارد
- بعد از نصب، ممکن است نیاز به نصب Command Line Tools اضافی باشد
- اگر خطای license دیدید، دستور بالا را اجرا کنید

---

### روش ۲: استفاده از نسخه Web (فوری - بدون نصب)

اگر نمی‌خواهید Xcode را نصب کنید، می‌توانید از نسخه Web استفاده کنید:

```bash
# باز کردن نسخه Web در مرورگر
open typing-practice.html
```

یا:
- فایل `typing-practice.html` را در Finder پیدا کنید
- دوبار کلیک کنید تا در مرورگر باز شود

**مزایا:**
- ✅ بدون نیاز به نصب
- ✅ همه ویژگی‌های نسخه macOS
- ✅ قابل استفاده در هر مرورگر
- ✅ فوری و آماده استفاده

---

### روش ۳: Build دستی در Xcode (اگر Xcode دارید)

اگر Xcode را نصب کردید:

1. **باز کردن پروژه:**
   ```bash
   open hhh.xcodeproj
   ```

2. **Build کردن:**
   - در Xcode: `Product > Build` (یا Cmd+B)
   - یا: `Product > Archive` برای نسخه Release

3. **Export کردن:**
   - `Window > Organizer`
   - Archive را انتخاب کنید
   - "Distribute App" > "Copy App"
   - محل ذخیره را انتخاب کنید

4. **نصب:**
   - اپلیکیشن را به Applications بکشید

---

## 🔍 بررسی وضعیت نصب

برای چک کردن اینکه Xcode کامل نصب است یا نه:

```bash
# چک کردن مسیر Developer
xcode-select -p

# اگر خروجی این بود:
# /Library/Developer/CommandLineTools
# یعنی فقط Command Line Tools دارید

# اگر خروجی این بود:
# /Applications/Xcode.app/Contents/Developer
# یعنی Xcode کامل نصب است ✅
```

---

## ❓ سوالات متداول

### آیا می‌توانم بدون Xcode build کنم؟

**خیر.** برای build کردن macOS app با SwiftUI، نیاز به Xcode کامل دارید. Command Line Tools کافی نیست.

### آیا Xcode رایگان است؟

**بله.** Xcode کاملاً رایگان است و از App Store قابل دانلود است.

### چقدر فضا نیاز دارم؟

حدود **12-15 GB** فضا برای نصب Xcode نیاز دارید.

### آیا می‌توانم از نسخه Web استفاده کنم؟

**بله!** نسخه Web همه ویژگی‌های نسخه macOS را دارد و بدون نیاز به نصب کار می‌کند.

---

## 🎯 توصیه

- اگر فقط می‌خواهید از اپلیکیشن استفاده کنید: **از نسخه Web استفاده کنید** (`typing-practice.html`)
- اگر می‌خواهید app نصب کنید: **Xcode را نصب کنید** و از `build.sh` استفاده کنید

---

**موفق باشید! 🚀**

